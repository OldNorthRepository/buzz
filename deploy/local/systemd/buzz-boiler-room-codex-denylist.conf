[Service]
# The actions mirror is the sole reply processor for this channel.  This UUID
# is intentionally non-secret; all identity material remains in the existing
# value-blind EnvironmentFile and render-key ExecStartPre.
Environment=BUZZ_ACP_IGNORE_CHANNELS=ad881e69-03a2-4bb8-b818-0cf46984f4d2

# Do not execute from an editable checkout.  The installer flips `current`
# atomically between immutable releases under ~/.local/libexec/buzz-acp.
ExecStart=
ExecStart=/home/laddy/.local/libexec/buzz-acp/gateway-denylist/current/buzz-acp
